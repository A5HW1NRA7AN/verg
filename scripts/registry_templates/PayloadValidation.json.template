{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "{{nameId}}": {
      "type": "string",
      "description": "Unique identifier for the {{name}} record"
    }
  },
  "required": [
    "{{nameId}}"
  ]
}
